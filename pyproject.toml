[project]
name = "qrproblem"
version = "0.1.0"
description = "GPU MODE QR Decomposition Kernel Competition"
requires-python = ">=3.12"
dependencies = [
    "torch",
    "numpy",
    "modal>=1.4.1",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
